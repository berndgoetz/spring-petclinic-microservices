spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: vets-service
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
---
spring:
  profiles: k8s
  cloud:
    config:
      enabled: false
    kubernetes:
      config:
        enabled: true
        name: pet-clinic-config